/target
**/*.rs.bk
Cargo.lock
.DS_Store

# Agent working state (context packages, prompts, drafts) -- never source.
# A committed gather artifact answers searches with another repo's details.
.cortexkit/
