[package]
name = "cortexkit-bus-trait"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "NATS-shaped message-plane traits and backend conformance contract"

[dependencies]
async-trait = "0.1"
sha2 = "0.10"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
